{
  "name": "jev-chrome-mcp",
  "version": "0.1.0",
  "description": "MCP server that runs the jev-browser-use click loop in Google Chrome.",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Pioneer113/jev-chrome-mcp.git"
  },
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test",
    "test:live": "node scripts/live-example.mjs"
  },
  "dependencies": {
    "playwright": "^1.63.0"
  }
}
